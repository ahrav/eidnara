# Property lens: wildcard

HEAD `2e4433e6b511ae74944df8a9669c428e73915d29`, 2026-09-13.
Scope and P notation: [source register](../source-register.md). This pass
follows all ten other property passes and the system wildcard.

Measurement bookkeeping can manufacture a pass. `events / 40 <= 16` at
P:L213 admits as many as 679 events under integer division. The intended
threshold is `events <= 640`. Whole-body native Values also make an
unattributed numerator incomparable to `messages` JSON bytes.

Tree-stage peak deltas cannot be added or maximized after separate resets:
the first stage's live tree must be present in the second stage's baseline.
Use one continuous interval and attribute inherited live allocations.
Allocation layout bytes remain a floor, not RSS or allocator residency.

One W1-style marker cannot mean either size point. The composite marker must
require complete before/after records for both 40 and 200 messages. This new
plan-specific evidence contract does not reactivate W1, whose invalidation
is explicit at the cited HEAD. No manifest or timing delta exists here.

Unique additions: strict threshold arithmetic, attribution validity, and
conjunctive evidence completeness. These refine the seven records rather
than adding a separate generic instrumentation property.
