# Independent portfolio evaluation and dispositions

Date: 2026-09-13. System: typed-wire-decode identity catalog.
HEAD: `2e4433e6b511ae74944df8a9669c428e73915d29`.
Baseline: `e451a2b470ae8663b4613ca04f019a30b6d7df53`.
External scope and source standing: [catalog.md](catalog.md#scope-and-provenance).

## Completed evaluation provenance

Fresh evaluator session: `ses_f6756093fffeVjNp36S3E8pKrM`.
The user reports completion of all four independent lenses and supplies the
findings applied here. The user also supplies a test-strategy correction about
`pair_byte_determinism_golden`. This file records that finding summary and its
dispositions, not a verbatim transcript or a new evaluator run.

The evaluator assessed the initial eight-record portfolio. Its recommendations
are verified against HEAD before application. Three targeted discovery passes
add the missing durable and numeric domains. They do not constitute another
independent evaluation or authorize changes to the settled plan or other
catalogs. All accepted constraints remain in force.

## Four-lens finding synthesis

| Completed lens | Finding carried into disposition | Classification |
| --- | --- | --- |
| Harness fit | Prefer exact value/byte and persisted-state oracles at existing seams; expose token/fold/protection outcomes rather than infer them from hashes. | refinement |
| Coverage balance | Durable hygiene parts/signature and lineage anchor hashes are missing; typed-only output coverage stops at the plugin's four output variants. | gap |
| Implementability | Each marker needs its own sometimes check; real old rows and policy-threshold inputs need explicit construction; a determinism test is not a literal byte pin. | refinement |
| Wildcard | Old sidecar normalization changes ordinary absent-flag plugin tools too; local native caches do not prove stamps cannot persist; broad frozen contracts cannot be waived. | gap and bias |

This synthesis organizes the supplied findings by lens. Source details and
exact proposed checks live in the linked evidence files, not in inferred
reviewer approval. Safety-heavy scope has no new bounded liveness promise;
independent situation checks address reachability without inventing one.

## Finding dispositions

| Finding | Class | Applied disposition | Remaining owner obligation |
| --- | --- | --- | --- |
| Missing durable hygiene domain | gap | Added `durable-hygiene-baseline-preserves-content-identity`, evidence, targeted lens, checks, and independent marker. | Hygiene owner supplies old-row replay and resolves daemon/legacy invalidation effects. |
| Missing durable lineage anchor | gap | Added `durable-lineage-anchor-preserves-validation` with stored-hash and actual fail-closed behavior. | Lineage owner freezes old valid/control outcomes; specification owner resolves unknown-envelope anchors. |
| Numeric byte consumers omitted | gap | Added `block-byte-policy-outcomes-remain-stable` with exact token, selection, fold, and protected/publication-floor outcomes. | Freeze P outcomes; establish real daemon-only reachability and resolve any semantic conflict. |
| Typed-only shape coverage incomplete | gap | Added independent `typed_wire_identity_typed_only_shapes` for all seven block and output variants, null denial reason, and optional forms. | Test strategy implements the separate typed matrix, not a reclassified plugin matrix. |
| Combined marker can obscure missing situations | refinement | Twelve independent named `sometimes` assertions; existing reachability record is reporting rollup only. | Each assertion needs its own observed input witness. |
| Determinism test described as a pin | refinement | Corrected catalog, evidence, and inventory after reading `injection.rs:794-831`: same-input equality and changed-input inequality, no literal baseline pin. | A true old/new frozen-byte oracle remains missing. |
| Normalized sidecar changes ordinary plugin tool flags | refinement | Recorded old `mark_modified` behavior, its explicit-false basis, and the prospective 26-byte omission for absent-flag plugin tools too. | Specification owner reconciles broad sidecar-preservation wording before implementation. |
| Persisted stamp assumption unproved | gap | Qualified process-local cache claims and added old stamped-row input to the sidecar witness. | Supply real provenance or a compatibility contract; do not assume serialized stamps vanish on restart. |
| W1 incorrectly assigned active ownership | refinement | Every identity-directory reference marks W1 invalidated; no measurement ownership or reactivation is asserted. | Any measurement requirement belongs in owner-authored work, not an implicit W1 revival. |
| Ten explicit-false golden blocks are not plugin preservation cases | refinement | Kept all ten classified; separate P preservation from accepted normalization controls. | Freeze missing absent/true/media cases without silently rebaselining the ten old cases. |
| Signed-zero equality mistaken for fresh byte equality | refinement | Retained separate equality/reuse and fresh-byte equations, including positional precedence. | Existing tests remain unaudited; replacement witnesses must preserve these semantics. |
| Blanket cache-only/frozen-behavior rationale | bias | Durable served receipts remain separate from added hygiene/anchor domains. No byte exception is widened into a semantic exception. | Specification owner settles all durable legacy and broad frozen-contract contradictions before implementation. |

Targeted discovery gaps are closed at the catalog-definition level. Their
implementation witnesses remain absent. Owner questions are explicitly open,
not dispositioned as permission to weaken constraints.

## Retained earlier findings and questions

- The isolated compact false-member delta is 26 bytes including its comma,
  not 25. A synthetic pair loses 52 block bytes from that omission alone.
  The Appendix's 26-of-53 count is corpus-specific. Parent wording remains an
  owner correction outside this directory.
- Production history_summarizer snapshots exclude synthetic blocks. The claimed todo
  snapshot delta needs another demonstrated path or a test-only classification.
- Durable served receipts include synthetic output. The first post-upgrade
  diagnostic result remains an owner question, not an empty-cache assumption.
- Unknown-envelope legacy ingress, baseline parts, and anchor identifiers can
  depend on bytes the plan removes. No migration or relaxed enforcement is
  invented here. These contradictions block implementation readiness.
- The initial discovery's nested-agent limit remains in its lens reports;
  the separate fresh evaluation is now completed, rather than still pending.

## Verification and evidence boundary

Eleven records remain active and `Exercised: not yet`. All existing checks
remain unaudited; correcting a description is not an adequacy verdict.
The catalog, evidence, check inventory, fault map, and handoffs include all
applied findings. Three targeted reports preserve the new source reasoning.

Mechanical checks cover schema/order, index/evidence correspondence, evidence
lengths, links, independent marker names, and HEAD source references. No code
tests, benchmarks, crash experiments, old/new decoder execution, or fresh
portfolio rerun occurred. Completed evaluation does not mean implementation
approval while the owner questions and execution evidence remain open.
