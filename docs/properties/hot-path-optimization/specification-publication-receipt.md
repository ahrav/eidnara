# Specification 351 publication receipt

- Specification: [Spec: behavior-preserving hot-path optimization](https://github.com/ahrav/eidnara/issues/351).
- Publication date: 2026-09-10.
- Repository: `ahrav/eidnara`.
- Issue state at verification: open, with no labels.
- Source baseline: `913234433ae36a80a6e22c6aac14c7f9aab74386`.
- Work-item marker: `hot-path-optimization-spec-a067609c-d67d-4377-9802-606638c9350f`.
- Approved body SHA-256: `adc840ce5e086215546b676c80e7be30b0ea34fb1750a5e602b19c4ea3854b15`.

The owner selected “Approve and publish” after seeing the full issue body,
title, repository, state, labels, testing seams, and exact mutation. Approval
confirmed the audit-based scope and proposed milestone graph, with no additional
external plan or incident evidence required to govern them. Pending-scope notes
in discovery records describe their discovery-time state; this receipt records
the subsequent decision for the five-area specification.

The marker search returned no matches before approval. One create command
returned the issue URL. A read-back verified the title, open state, empty label
set, exactly one marker, and a byte-identical SHA-256 match to the approved body.
The temporary issue-body file was removed after creation.

## Property and review evidence

The reviewed five-area supplement contains 16 records and matching evidence:
K1-K4, E1-E3, S1-S2, H1-H4, and R1-R3. Before publication, a mechanical check
validated the schema, record/evidence correspondence, and 433 local links and
anchors across the 26 documentation files then in this supplement.

Independent reviewer session `ses_f75dcfb2affeJX2JsQztNWN1aP` evaluated the
initial catalog, then rechecked the revised 16-record catalog and specification.
Corrections separate row/byte and inner/outer pressure witnesses, SOFT/refold
semantics, current resource classes, fixed reference identity, and functional
audit equivalence from allocation measurement. The final two specification
wording corrections distinguish both visibility boundaries and oldest-first
guard demotion. The owner then approved the exact corrected body.

Implementation gates remain open for excluded-row corruption behavior, worker
ownership and numerical capacity, compatible callback grouping, and performance
workloads and policy. Documentation review does not close these gates or prove
runtime correctness. No test, fault, or performance campaign ran.

## Concurrent artifact boundary

During publication, an additional `latency-audit/` directory appeared and shared
portfolio files changed. Those edits are outside this specification task's
approved five-area scope. They were preserved, not folded into issue 351 or
overwritten to restore an earlier review document. This receipt records the
publication evidence independently of those concurrent edits.

Only this specification issue was created. No implementation ticket, PR,
commit, source change, or test change was created by this task. The reusable
catalog remains local working-tree documentation for a separately authorized
commit.
