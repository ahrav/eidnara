name: ci

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  ci:
    name: ci
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.3.14

      - name: Install TypeScript tooling
        run: bun install --frozen-lockfile

      - name: Typecheck migration tooling
        run: bun run typecheck

      - name: Migration tooling tests
        run: bun run test:eidnara-migration

      - name: Registry
        run: bun run eidnara:check registry migration/registry.json

      - name: Waivers
        run: |
          for file in migration/waves/*/waivers.json; do
            bun run eidnara:check waivers "$file"
          done

      - name: Source checkouts for receipt verification
        run: |
          set -euo pipefail
          repos=$(bun -e 'const fs=require("fs");const s=new Set();for(const f of fs.readdirSync("migration/waves")){const p=`migration/waves/${f}/receipt.json`;if(!fs.existsSync(p))continue;for(const r of JSON.parse(fs.readFileSync(p,"utf8")).sources??[])s.add(r.repo)}console.log([...s].join(" "))')
          for repo in $repos; do
            git clone --quiet --no-checkout "https://github.com/ahrav/${repo}.git" "../${repo}"
          done

      - name: Receipts
        run: |
          set -euo pipefail
          args=()
          for dir in ../*/; do
            name=$(basename "$dir")
            [ -d "$dir/.git" ] && [ "$name" != "$(basename "$PWD")" ] && args+=(--checkout "${name}=${dir%/}")
          done
          for file in migration/waves/*/receipt.json; do
            bun run eidnara:check receipt "$file" "${args[@]}"
          done

      - name: Property catalog indexes
        run: |
          shopt -s nullglob
          parts=(docs/properties/*/catalog.md)
          if [ "${#parts[@]}" -eq 0 ]; then
            echo "no property catalogs yet"
            exit 0
          fi
          for catalog in "${parts[@]}"; do
            part=$(dirname "$catalog")
            bun scripts/eidnara-migration/generate-property-index.ts "$part" --check
            bun run eidnara:check property-catalog "$part/index.json"
          done

      - name: Rust toolchains
        run: |
          rustup toolchain install 1.89 --profile minimal --component clippy,rustfmt
          rustup toolchain install stable --profile minimal --component clippy,rustfmt

      - name: Rust format
        run: cargo +1.89 fmt --all -- --check

      - name: Rust lint (1.89)
        run: cargo +1.89 clippy --workspace --all-targets --all-features --locked -- -D warnings

      - name: Rust lint (stable)
        run: cargo +stable clippy --workspace --all-targets --all-features --locked -- -D warnings

      - name: Rust tests (1.89)
        run: cargo +1.89 test --workspace --all-targets --all-features --locked

      - name: Rust doctests and docs (1.89)
        run: |
          cargo +1.89 test --workspace --doc --all-features --locked
          RUSTDOCFLAGS="-D warnings" cargo +1.89 doc --workspace --no-deps --all-features --locked

      - name: Rust tests (stable)
        run: cargo +stable test --workspace --all-targets --all-features --locked

      - name: Feature configurations (1.89)
        run: |
          cargo +1.89 check --workspace --no-default-features --locked
          cargo +1.89 check --workspace --all-features --locked

      - name: MSRV with unlocked latest dependencies
        run: |
          set -euo pipefail
          scratch=$(mktemp -d)
          git archive HEAD | tar -x -C "$scratch"
          cd "$scratch"
          cargo +1.89 update
          cargo +1.89 check --workspace --all-targets --all-features

      - name: Workspace graph has no sibling checkout or metadata stub
        run: |
          set -euo pipefail
          # Every path dependency must resolve inside this checkout. A sibling
          # checkout (../commons) or a metadata-only stub resolves outside it.
          cargo +1.89 metadata --locked --format-version 1 \
            | bun -e '
              const meta = JSON.parse(await Bun.stdin.text());
              const root = meta.workspace_root + "/";
              const outside = meta.packages
                .filter((p) => p.source === null && !p.manifest_path.startsWith(root))
                .map((p) => p.name + " -> " + p.manifest_path);
              const stubs = meta.packages
                .filter((p) => p.source === null && p.targets.length === 0)
                .map((p) => p.name);
              if (outside.length || stubs.length) {
                console.error("path dependencies outside the workspace:", outside);
                console.error("packages without targets:", stubs);
                process.exit(1);
              }
              console.log("workspace graph: " + meta.packages.filter((p) => p.source === null).length + " local packages, all inside " + root);
            '

      - name: Forbidden references
        run: |
          set -euo pipefail
          # Cross-repo CI stubs and legacy backfill paths never enter the destination tree.
          if grep -rln 'provision-rust-ci-stubs\|storage-v22-backfill' --exclude-dir=.git --exclude-dir=node_modules --exclude-dir=target --exclude=ci.yml . ; then
            echo "cross-repo stub or backfill path found"; exit 1
          fi
