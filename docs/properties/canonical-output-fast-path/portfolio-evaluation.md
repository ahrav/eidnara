# Portfolio evaluation status

**Independent final evaluation complete: accept, no blockers.** Separate
analyst task `ses_f675203e4ffevgEH7CseOxb6jK` returns six refinements, one seam
gap, and one framing bias. All eight are dispositioned below; no blocking
discovery finding remains. This accepts the property portfolio, not an
implementation or performance result. No runtime evidence exists.

## Evidence and provenance

The user explicitly supplied the settled plan as the evidence scope. Three
agent discovery lanes supplied the reports. No additional incidents were
supplied. No fictional “none” interview answer is recorded. The main agent
reports fetching GitHub
[#350](https://github.com/ahrav/eidnara/issues/350) and
[#441](https://github.com/ahrav/eidnara/issues/441) on 2026-09-13: both OPEN,
no comments. Those references delimit HP1/direct publication, not new scope.

Source checks use `git show HEAD:<path>` at
`2e4433e6b511ae74944df8a9669c428e73915d29`; plan baseline `4980f8af` is
distinct. Initial independent analyst findings are retained in
[_lenses/portfolio-baseline.md](_lenses/portfolio-baseline.md). Writer wildcard
verification and rejected allegations are retained in
[_lenses/wildcard.md](_lenses/wildcard.md). Peer reports and historical catalogs
remain unchanged.

The initial report is an independent agent evaluation, preserved with its
original findings and limits. The later final evaluation is separate. The
main agent supplied its result to this writer; neither reviewer is credited
with tests or benchmarks. The final reviewer reports checking roughly forty
cited source claims at pinned HEAD and confirming the exact field schema/order,
nine records/evidence files, and six `always`, one optional, two `sometimes`
semantics. Existing tests remain unaudited for invariant strength.

## Dispositions applied before final evaluation

| Class | Finding | Applied disposition |
| --- | --- | --- |
| Gap | Fixed B escapes slope checks. | S5 requires absolute allocation/size/lifetime attribution and logical copies. |
| Refinement | Global allocator can count harness work. | Explicit isolated libtest/nextest invocations plus owner-thread nonallocating recording; mutex alone rejected. |
| Gap | Identity bridge and campaign situations are missing. | S3 provides real-table identity; C1/C2 require all independent preconditions across a campaign. |
| Refinement | Observation apparatus can exceed the production change. | No duplicate encode setup, production hooks/counters, new API, or giant geometry validator. Source verification plus late-disorder literals is allowed. |
| Refinement | Reachability was too broad or too narrow. | Default empty HarnessMeta and ordered block parent are production witnesses; exact ordered message root, duplicate emission, and generic error states retain private-test limits. |
| Refinement | Inspection was called partial exercise. | All nine records say not yet; every existing check remains unaudited. |
| Refinement | Value oracle and diagnostics have conditions. | Preserve feature-independent algorithm, literals/decoded permutations, and same-variant/partition error comparisons. |
| Rejected allegation | content_mut loses untouched sibling unknown fields. | Independent block originals refute it; preserve the B1 witness without a new defect record. |
| Gap | Isolated canonicalizer/full-constructor and real-host timing evidence are absent. | Plan U0/U4 retains measurement work; host driver only if claiming user-visible latency. |
| Bias | Fixture sizes and local allocation savings may be overgeneralized. | No claim of empirical production representativeness, lower constructor peak, or latency gain. |
| Boundary | B1/T1–T4 and invalidated W1/W2 already have owners. | Relationships only; no duplicates, reactivation, direct caller, extra timing field, or transport fault campaign. |

## Final independent findings and dispositions

| ID | Class | Finding and verified evidence | Disposition |
| --- | --- | --- | --- |
| G1 | Seam gap | The [integration entry][entry] measures canonicalization only. The [constructor][constructor] is private, and HEAD has allocator declarations only in [parse-charge][peak-alloc] and [canonicalizer][event-alloc] integration fixtures, not the unit binary. | Resolved with a feasible private in-crate route, not executed proof. U0 searches/reuses allocator fixtures and establishes the isolated observer described below before U1. No public constructor wrapper is needed. |
| G2 | Refinement | [Live-tail lookup][tail] increments hits for `Some(None)`; [synthetic lookup][synthetic] flattens it into a miss. Counters alone cannot prove a positive warm item. | S6 and C2 explicitly require the retained `Some(Some(served))` value. `warm_positive_tail` excludes omitted entries independently of counters. |
| G3 | Refinement | [Cargo.lock][lock] pins serde_json 1.0.151 with `itoa`, `memchr`, `serde`, `serde_core`, and `zmij`, but no `indexmap`. | The lock snapshot plus manifest supports the inspected locked map assumption, not a runtime-captured feature graph. Keep the algorithm feature-independent and never require preserve_order off. |
| G4 | Refinement | The production ordered block parent/disordered child requires traversal, but the typed message root is already disordered. | S2/C1 retain both witnesses. Only the private ordered-root/child-only case isolates the false-negative root-only aggregate. No new property is needed. |
| G5 | Refinement | Reachability must follow the nonvacuous obligation, not every auxiliary probe. | S1 stays default-production because real objects owe its core permutation rule; private duplicate probes do not change that. S4's error clause requires private failing inputs, so it stays test-only. |
| G6 | Refinement | An opaque accumulated AND failure obscures which situations were missed. | C1/C2 require each missing marker and incomplete composite member to be reported separately. Markers still observe independent preconditions, never defects. |
| G7 | Framing bias | The portfolio follows the selected plan and can overgeneralize a local allocation saving. [Constructor order][constructor] retains A through fingerprints/hash before Arc conversion. | Surface the bias and preserve settled user scope. B1 owns final Arc payload bytes/length; transient A slack ends at conversion, but unchanged retained accounting is not global RSS. Peak is unknown and no universal residency win is claimed. Existing U4 investigation covers design changes prompted by measurement; no new record or transport work is required. |
| G8 | Refinement | The complete envelope writer is [dispatch.rs:343–371][envelope]; the frozen-corpus attribute at 13759 and function at 13760 are both valid anchors. | Correct the touched S7 envelope link to the whole function. Keep the valid corpus attribute/function anchors and historical evidence unchanged. |

### G1: Full-constructor observer route

The existing [transform module test][shell-test] calls the private constructor
directly. Use that seam in an isolated filtered in-crate test process, with
nonallocating thread-owned test-only allocator recording. Search and reuse
compatible allocator-fixture logic first; the process-wide PeakAlloc mutex is
not sufficient to exclude harness allocations. Keep fixture/reference work
outside the interval and observe the whole constructor through Arc conversion.

U0 must implement and verify this observer before U1. The route requires no
public constructor wrapper/API, production hook, disabled cfg(test) fresh
differential, new harness framework, or new dependency. If no compatible
test-only observation can be established, stop U0 and obtain seam approval
rather than weaken the peak gate. Integration visibility alone is not an
unresolved human decision; private test access is verified. Actual observer
behavior and peak measurements remain unverified.

### Evaluation outcome by lens

- **Harness fit:** G1 closes the discovery seam gap with an in-crate route;
  observation implementation and isolation validation remain U0 work.
- **Coverage balance:** G2, G4, G5, and G6 refine positive-hit evidence,
  traversal witnesses, nonvacuous reachability, and missing-marker reporting.
- **Implementability:** G3 and G8 tighten the locked-map evidence and exact
  source anchors without adding production interfaces or feature restrictions.
- **Wildcard:** G7 makes the plan-shaped framing bias explicit and distinguishes
  final payload invariants, transient constructor memory, and global RSS.

## Unrun implementation work and handoff

- Candidate implementation, runtime exercise, negative controls, and all plan
  implementation gates are absent from this documentation pass.
- Absolute allocator recording, returned-A identity, and full-constructor peak
  through receipt construction/hash/Arc conversion remain unmeasured. U0 must
  establish the private in-crate observer before U1.
- Canonical/miss/hit frequencies and CPU/latency comparisons remain unknown.
- Missing path-specific diagnostic and campaign assertions still need tests.
- A reproducible real-transform host driver is absent; require it only for a
  user-visible latency claim. No direct publication work is delivered.

All nine records go to `/testing:test-strategy`; existing tests go separately
to `/testing:invariant-test-review`, production guards to
`/low-level-systems:defensive-assertions-and-invariant-guards`. Final independent
portfolio evaluation is complete. W1/W2 stay invalidated; T3/T4 stay with their existing
owners and are not marked delivered or exercised here.

## Mechanical verification boundary

Mechanical checks on 2026-09-13 confirm nine records, nine index rows, and nine
matching evidence files; six `always`, one `always-or-unreached`, and two
`sometimes`. Every record has METHOD's exact field order and a not-yet-exercised
status. Evidence files have the required section order. S5 retains additional
detail for the verified constructor seam and the unimplemented observer gate.
Relative file/heading links resolve; immutable source ranges are within their
verified HEAD blobs. A direct whitespace scan covers all 15 new files because
ordinary `git diff --check` does not include untracked additions.

These mechanical checks are distinct from the independent final evaluation
above. Neither establishes runtime property satisfaction. All records remain
not yet exercised, and all existing checks remain unaudited. No source/test/CI/
tracker or peer-lens edits are made.

[entry]: https://github.com/ahrav/eidnara/blob/2e4433e6b511ae74944df8a9669c428e73915d29/crates/daemon/src/served_json.rs#L111-L119
[constructor]: https://github.com/ahrav/eidnara/blob/2e4433e6b511ae74944df8a9669c428e73915d29/crates/daemon/src/transform.rs#L164-L224
[peak-alloc]: https://github.com/ahrav/eidnara/blob/2e4433e6b511ae74944df8a9669c428e73915d29/crates/daemon/tests/parse_charge_covers_typed_decode.rs#L25-L87
[event-alloc]: https://github.com/ahrav/eidnara/blob/2e4433e6b511ae74944df8a9669c428e73915d29/crates/daemon/tests/served_json_passthrough_allocations.rs#L10-L32
[tail]: https://github.com/ahrav/eidnara/blob/2e4433e6b511ae74944df8a9669c428e73915d29/crates/daemon/src/transform.rs#L11127-L11131
[synthetic]: https://github.com/ahrav/eidnara/blob/2e4433e6b511ae74944df8a9669c428e73915d29/crates/daemon/src/transform.rs#L10405-L10422
[lock]: https://github.com/ahrav/eidnara/blob/2e4433e6b511ae74944df8a9669c428e73915d29/Cargo.lock#L2828-L2839
[envelope]: https://github.com/ahrav/eidnara/blob/2e4433e6b511ae74944df8a9669c428e73915d29/crates/daemon/src/dispatch.rs#L343-L371
[shell-test]: https://github.com/ahrav/eidnara/blob/2e4433e6b511ae74944df8a9669c428e73915d29/crates/daemon/src/transform.rs#L13713-L13757
