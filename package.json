{
  "name": "eidnara",
  "private": true,
  "license": "MIT",
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "@types/bun": "1.3.14",
    "ai-tokenizer": "1.0.6",
    "typescript": "5.9.3"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json && bun run --cwd packages/shm-native typecheck",
    "lint": "true",
    "test": "bun run --cwd packages/shm-native test",
    "build": "bun run --cwd packages/shm-native build:js",
    "check:repo": "bun run typecheck && bun run lint && bun run test && bun run build"
  }
}
