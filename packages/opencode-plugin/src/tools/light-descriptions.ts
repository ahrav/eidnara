export const EIDNARA_REDUCE_LIGHT_DESCRIPTION = `For eidnara_reduce users, mark spent §N§ outputs discardable; release is QUEUED, not immediate, so content stays visible until space is needed. Newest tags stay protected until they age out. Released content becomes a placeholder; recover it only by rerunning the source or recovery tool, so mark only genuinely finished material. Mark analyzed, redundant, persisted, or merely confirmatory outputs; keep user messages, unresolved errors, unextracted evidence, and exact wording. NEVER blanket-mark a large range: review every tag first.`;

export const EIDNARA_NOTE_LIGHT_DESCRIPTION = `For eidnara_note users, write saves, read lists, update changes, and dismiss retires future session notes; surface_condition creates a conditional note. For conditional notes, NEVER use a condition tied to this conversation or an unobservable future action because the background checker can inspect only external GitHub, disk, git, or web signals.`;

export const EIDNARA_MEMORY_LIGHT_DESCRIPTION = `For eidnara_memory users, create one durable project claim with category and content; revise, archive, or merge claims by opaque object id, or get up to twenty object ids. No token is passed; revise and merge return the survivor's id. list remains memory_classifier-only, so primary agents must NEVER assume bulk-list access.`;

export const EIDNARA_SEARCH_LIGHT_DESCRIPTION = `For eidnara_search users, search the project's durable memories served by the memory daemon, including rejected-approach warnings. A query that is entirely memory object ids (\`mem_<32hex>\`) resolves those memories directly; other queries rank by text. A lagging or absent daemon is reported in the result.`;
