# Portfolio evaluation: completed

System: `/local/home/ahrav/scratch/eidnara`.
Source HEAD: `2e4433e6b511ae74944df8a9669c428e73915d29`.
Evaluation and disposition date: 2026-09-13.
Independent analyst: `ses_f6756093fffeVjNp36S3E8pKrM`.
External scope and source provenance are in
[catalog.md](catalog.md#scope-and-provenance).

The user supplies the completed fresh-context analyst findings and an explicit
scope amendment. This document records that independent pass and its local
disposition. It does not represent another analyst invocation or a test run.
The initial discoverer's inline lenses remain preserved as discovery evidence.

The four-lens evaluation is complete. Refinements are applied below; the
cross-work integration gap has a named owner and remains open. All seven
properties retain `Exercised: not yet`. Existing checks remain unaudited.
No tests, builds, benchmarks, source changes, or tracker writes accompany
this disposition. Specification publication requires final approval.

## Lens 1: harness fit

The raw-value-token hypothesis is source-backed and belongs in a
discriminating baseline/final comparison through the existing byte and handler
entry seams. Agreement between two final lanes alone cannot establish
preservation. No supplied run confirms a defect; a separate review's
confirmed-defect wording is therefore not adopted.

Explicit-null serializer_profile is a boundary-preservation case. Preserve
the existing Option wrapper and handler behavior. Adding the missing witness
does not authorize the deferred TransformRequestWire wrapper refactor.
Test strategy retains ownership of concrete test forms and instrumentation.

## Lens 2: coverage balance

The seven-record portfolio covers envelope representation, fallback parity,
payload retention, page/non-transform behavior, ownership, mutation/equality,
and independent fallback reachability. No liveness record is justified for
this synchronous deterministic slice. Missing such a record is not itself a
gap; inventing a timeout would test a different contract.

The fallback reachability record needs both message and block witnesses.
They remain one catalog record but constitute two independent `sometimes`
obligations. A campaign summary must preserve each result rather than allowing
one frequent marker to conceal the other's absence.

## Lens 3: implementability

For each fixed fallback marker, observe all of: literal unpaged transform
bytes, accepted compatibility gate, Invalid typed decode, successful
independent tree conversion, and BodyLane::Tree. Maintain separate witnessed
bits and require their conjunction at campaign completion. An OR or total-hit
count does not satisfy this condition.

The class remains default-production for the owning handler path. CK/block
duplicate failure is prospective until derived serde exists; this label is
not execution evidence. The marker seam and baseline/final run remain test
implementation work, with every claim unexercised.

## Lens 4: wildcard

Worktree-only [TE08](../../transform-edit-responses/catalog.md#cached-canonical-prefix-preserves-exact-ingress)
requires exact captured CK ingress, including unknown fields, while accepted
R3 discards those fields. This specification's owner preserves R3 and records
the integration owner's reconciliation before combining the work. The other
agent's untracked catalog is not changed by this disposition. Integration
reconciliation remains open; no compatibility exception is authorized.

The settled plan and explicit user amendment supply scope without another
interview. Publication requires final approval; it is not forbidden. The added
`pub mod search_seed;` line explains the +1 offset for following daemon lib.rs
references against `e451a2b4`; it does not make all plan references wrong.

## Finding disposition

| Finding | Class | Disposition and owner |
| --- | --- | --- |
| Raw-token parity hypothesis | refinement | Applied in the fallback record/evidence and fault map. Require baseline/final discrimination; implementation owner stops and reports any acceptance change. No defect or exception is asserted. |
| Explicit-null serializer_profile | refinement | Applied as preservation coverage. Test-strategy owner adds a witness while leaving the deferred wrapper refactor outside this work. |
| Two fallback marker results | refinement | Applied in catalog, evidence, and fault map. Test-strategy owner independently enforces both `sometimes` results; aggregate reports cannot mask either. |
| TE08 exact-ingress conflict | gap | Recorded in the R3 record/evidence and cross-work handoff. Specification owner preserves R3; integration owner records reconciliation before integration. Open integration obligation, not deferred discovery. |
| Scope and publication wording | refinement | Applied. Settled material plus amendment supplies scope; final publication approval remains required. |
| Source-reference offset | refinement | Preserved and clarified. The added module line explains following daemon references' +1 offset. |
| Pressure to add liveness | bias check | No liveness property is justified by the synchronous deterministic scope. No additional record is added to balance counts. |
| Pending-review receipts | refinement | Replaced with this dated completed pass. Initial mechanical checks remain explicitly historical. |

## Open execution and integration questions

- Does the required baseline/final raw-token fixture exhibit an acceptance
  change? If it does, stop and report. No compatibility exception is authorized.
- How will the integration owner reconcile TE08 with R3 before integration,
  while preserving R3 as this specification's accepted decision?
- Which existing test seam will capture and report each fallback marker
  independently? The final campaign must require both results.
- The initial catalog's remaining test-construction questions and W1
  measurement-ownership handoff remain open with their existing owners.

Evaluation completion is a review milestone, not proof of implementation
correctness, executed coverage, or authorization to publish the specification.
