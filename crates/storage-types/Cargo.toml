[package]
name = "storage-types"
version = "0.1.1"
edition = "2021"
rust-version.workspace = true
license.workspace = true
repository.workspace = true
publish.workspace = true
description = "Shared storage descriptor and migration types."

[dependencies]
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
serde_json = "1"
