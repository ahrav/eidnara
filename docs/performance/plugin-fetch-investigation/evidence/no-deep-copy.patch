diff --git a/crates/daemon/src/kernel_routes/read.rs b/crates/daemon/src/kernel_routes/read.rs
index e6695ec9..eaa1bb41 100644
--- a/crates/daemon/src/kernel_routes/read.rs
+++ b/crates/daemon/src/kernel_routes/read.rs
@@ -345,16 +345,13 @@ impl HandlerCore {
                 }
             }
         }
-        kernel_response(
-            &KernelOutcome::Available,
-            json!({
-                "known_as_of": response.known_as_of,
-                "tip": response.tip,
-                "gated": parsed.gated,
-                "truncated": truncated,
-                "rows": rows,
-            }),
-        )
+        let mut body = serde_json::Map::new();
+        body.insert("known_as_of".to_string(), json!(response.known_as_of));
+        body.insert("tip".to_string(), json!(response.tip));
+        body.insert("gated".to_string(), json!(parsed.gated));
+        body.insert("truncated".to_string(), json!(truncated));
+        body.insert("rows".to_string(), Value::Array(rows));
+        kernel_response(&KernelOutcome::Available, Value::Object(body))
     }
 }
 
