# Redaction-ownership surface

This summarizes separate prior read-only surface discovery supplied in the
task, not a new test run or portfolio evaluation. Anchors are rechecked in
`/local/home/ahrav/scratch/eidnara` at
`913234433ae36a80a6e22c6aac14c7f9aab74386` on 2026-09-10. The
[external-evidence scope](../catalog.md#scope-and-provenance) is pending final
confirmation; historical citations and exercise are not carried forward.

[Field preparation][prepare] clones redacted Content output but retains the
redacted allocation in its scan. NewIdentity refuses detected input before
scan append; ExistingIdentity preserves input and records preserve actions.
Durable and Transaction layers make six policy combinations. Applied clean
NewIdentity fields produce zero-finding field_scans and no scan_detections rows.

[Audit persistence][audit] consumes detections, owners, field identifiers, and
detector provenance, not retained redacted text. [JSON-observed scans][json]
already store empty text with their detections. R1-R3 separate functional
metadata/effect parity from the allocation-saving hypothesis. R3 compares the
observable audit projection for equal metadata, not an internal struct shape.
Sentinel privacy is an existing contract, not proof of allocation removal.

Both input and prepared output meet the 512 KiB bound before append. The
privacy witness is a unique long synthetic sentinel in substituted content
and payload-free audit fields, not arbitrary substring absence. Preserved
identities retain their existing exception. The redundant allocation is
already redacted; removing it is not itself a security repair. No observable
contract depends on String object identity.

[prepare]: ../../../../crates/memory-store/src/lib.rs#L2198-L2237
[audit]: ../../../../crates/memory-store/src/lib.rs#L2344-L2418
[json]: ../../../../crates/memory-store/src/lib.rs#L2127-L2137
